import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.roman.dopaminereset",
  appName: "DopamineResetCoach",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
